<%= form_for [:admin, @micropost] do |form| %>
  <% if @micropost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@micropost.errors.count, "error") %> prohibited this micropost from being saved:</h2>

      <ul>
        <% @micropost.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <label class="sr-only">Title</label>
    <%= form.text_field :title, class: "form-control", id: "title", placeholder: "Title" %>
  </div>

  <div class="form-group">
    <label class="sr-only">Content</label>
    <%= form.text_area :content, class: "form-control summernote", placeholder: "Content" %>
  </div>

  <div class="clearfix">
    <%= form.submit :class => "btn btn-primary pull-right" %>
  </div>
<% end %>
